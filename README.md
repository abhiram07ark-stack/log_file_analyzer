# log_file_analyzer
This project is a simple Python program that reads a log file and counts the total number of error messages present in it. The program looks for the keyword "ERROR" in each line of the log file and keeps track of how many times it appears.
